services:
  - type: worker
    name: emovies-news-telegram-bot
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    envVars:
      - key: TELEGRAM_BOT_TOKEN
        sync: false
      - key: POLL_INTERVAL_SECONDS
        value: "2700"